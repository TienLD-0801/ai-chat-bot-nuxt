version: '3.1'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: app
    restart: always
  nginx:
    image: nginx:latest
    restart: always
    volumes:
      - ./docker/nginx:/etc/nginx/conf.d
    depends_on:
      - app
    ports:
      - "80:80"
