version: '3.1'
services:
  bnk-chat-bot:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: bnk-chat-bot
    image: bnk-chat-bot
    restart: always
    ports:
      - "3500:80"